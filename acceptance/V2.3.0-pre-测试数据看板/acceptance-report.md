# 验收报告 · 统计-测试数据看板（v2.3.0 先行版）

> 环境：测试环境 `10.67.8.183:7777`（开发环境水印）· 账号：邓欣羽（#475，非 QA 名单）· 验收日期：2026-07-24
> 说明：本项为 v2.3.0 先行验收，非正式发版轮，**不生成发版初稿、不做回归沉淀**。
> 回归徽章：`@project_publish` 12 过 / `@data_export` 5 过 / api 冒烟 37 过 —— **全绿**（明细见附录 A.1）。

入口：统计 → 右侧栏「缺陷」分组 → **测试数据看板**（`/statistic/bug`）。页面双 tab：**测试数据总览** / **BUG明细**。

---

## 一、验收结论（逐功能）

### 1. 顶部筛选条 + 统计周期切换 ✅满足

统计周期支持 月 / 季度 / 半年 / 年，联动日期选择器与全部区块。

验证证据：
- UI ✅ 切「季度」→ 日期变「2026 Q3（进行中）」，标题「2026 Q3 环比 2026 Q2」，三大区块同步刷新；切回「月」恢复。
- 边界 ✅ 进行中周期显式标「（进行中，数据截至今日）」；上期无数据时展示「不可比 / 上期无数据」而非错误数字。
- 接口 ✅ 周期切换触发 `get_qa_stat_kpi/bug/publish/summary`，`period_key` 随粒度切换为 `2026-07`/`2026Q3`/`2026H1`/`2026`。
- 数据 ✅ **四种粒度共 8 个周期逐一算数核验全对**（见附录 A.4）：月(2026-07/06)、季度(2026Q3/Q2)、半年(2026H1/H2)、年(2026/2025)——每周期均满足「KPI↔PUBLISH 两接口同源、率手算=展示、按时=正常+提前、BUG维度 total=状态段和」四道校验，且环比基准链正确（进行中周期 prev 精确等于上一同粒度已结束周期实测值）。

### 2. 人员维度筛选（部门 / 归属人 / 测试人员 / PM）✅满足（设计口径：BUG 筛选只影响 BUG 维度）

四个筛选器均为可搜索下拉，选中后 BUG 维度按条件重算、可清空还原；**递交维度分析不响应 BUG 归属类筛选（设计如此：BUG 筛选只影响 BUG，递交只受统计周期影响）**。首轮验收时存在的「递交维度被 BUG 筛选清零」问题（原 B1）已修复销账，见「复验记录」。

验证证据（2026-07-27 修复后实测）：
- UI ✅ 选「项目开发」部门 → BUG维度=3（正确过滤），递交维度分析保持全量 65（符合设计）；清空还原正常。
- 边界 ✅ 无命中数据时 BUG 维度展示空态文案，无报错。
- 接口 ✅ `dept_id`/`assigned_to`/`pm_id` 参数下发 BUG 类接口；`assigned_to=999999`（不存在）→ code 0 返全 0 不报 500；`dept_id=-1` → code 0 优雅处理。
- 数据 ⚠️ 小残留：顶部「关键指标环比概览」的部分递交小卡（准时递交率/正常率/延期数）仍透传 BUG 筛选显示 0/0，与递交维度分析区同屏不一致（超时卡已正常显示 4 项），待统一，见复验记录与 fix-handoff 第 2 轮。

### 3. 关键指标环比概览 ✅满足

递交数据环比（准时/正常/提前/临时/延期/超时）+ Bug 数据环比（严重/致命/总数）+ 目标达标判定。

验证证据：
- UI ✅ 准时递交率 96.92%「已达标」、致命 BUG「降低 100%」等达标标记正确；上升/下降箭头与色彩区分。
- 边界 ✅ 「目标 ≥ 90%」「降低 50%」等目标阈值展示；prev=0 时标「上期为 0，不可比」。
- 接口 ✅ `get_qa_stat_kpi` 返回 `summary`+`targets`，`is_default_target` 标识默认目标。
- 数据 ✅ 准时递交率 = 按时 63 / 总数 65 = 96.92% ✔；正常率 76.92% = 50/65 ✔；提前率 20% = 13/65 ✔。

### 4. BUG 维度分析 ✅满足

总览数据（BUG总数/缺陷浓度/平均修复工时）+ 类型分布饼图 + 自检分布 + 责任相关（部门/指派人/PM/项目 TOP）+ 属性分布（行业线）。

验证证据：
- UI ✅ 各区块渲染完整，图表带说明副标题，可「收起/展开」。
- 边界 ✅ 「AI分析（后续规划补充）」区标「规划中」占位，无空指针。
- 接口 ✅ `get_qa_stat_bug` 返回 8 个维度键（overview/bug_type_distribution/self_check_distribution/dept_top5/assignee_top10/pm_stacks/project_top10/h_line_avg_stacks）。
- 数据 ✅ **交叉一致**：BUG总数 6 = 状态段（已解决 6 + 进行中 0 + 未解决 0 + 未定义 0）= 源头接口 `bug/get_list` total 6 ✔；缺陷浓度 1.20 = 6/5（涉及项目数）✔；平均修复工时 0.58 = 3.5h/6 ✔；类型饼图 程序功能 3 + 场景美术 2 + 项目技术 1 = 6 ✔。

### 5. 图表下钻明细 ✅满足

点击状态分项数字 / 饼图扇区 / 柱段 → 弹出对应筛选的 BUG 明细弹窗（带分页）。

验证证据：
- UI ✅ 点「各类型BUG数量占比」饼图首扇区 → 弹「明细 — 类型 · 程序功能 · 共 3 条」；点状态「已解决」→「明细 — BUG · 已解决 · 共 6 条」。
- 边界 ✅ 分页控件正常（20条/页），单页数据翻页禁用态正确。
- 接口 ✅ 下钻走 `get_qa_stat_detail_list?chart_key=bug_status&option_id=resolved`；非法 `chart_key=hack` → code 50「无效的 chart_key」；负 limit / 超大 page → code 0 不报错返回全集。
- 数据 ✅ 下钻明细条数与图表分项数字一致（程序功能 3 / 已解决 6）。

### 6. 递交维度分析 ✅满足

递交数据概览（涉及项目/递交总数/按时/提前/延期/超时/临时）+ 延期交付清单 + 延期责任分布 + 临时递交类型 + PM 递交数据表。

验证证据：
- UI ✅ 各数字块渲染，PM 递交数据表按递交次数排序（晏凯 22 / 张小杰 14 …），列含超时率/提前率/临时递交率。
- 边界 ✅ 筛选归零后延期清单显「本周期无延期交付」空态。
- 接口 ✅ `get_qa_stat_publish` overview 与 KPI 同源（total 65 / ontime 63 / delay 1 / over_tb 4 / temporary 12）。
- 数据 ✅ **交叉一致**：递交总数 65 = 按时 63 + 延期 1 + 超时? 口径见风险 R2；按时 63 = 正常 50 + 提前 13 ✔；临时占比 18.46% = 12/65 ✔。

### 7. BUG 明细 tab（增删改流转）✅满足（当前账号读态）

第二个 tab，逐条查看/新增/流转 BUG，含 添加Bug / 导出BUG 按钮 + 明细表。

验证证据：
- UI ✅ 6 条记录渲染，列含测试人员/指派给/操作描述/BUG类型/影响程度/提交时间；提交时间此处显示正常（2026-07-23 等）。
- 边界 ✅ 「添加Bug」对当前账号 disabled——门禁逻辑 `isCanAddBug`：非黑名单且不在 `test_user_list` 才可加；邓欣羽 systemRole=GHOST、`isCanAddBug=true`（本次 disabled 系首屏 `test_user_list` 未就绪的短暂态，刷新后恢复可点，非缺陷，见风险 R3）。
- 接口 ✅ `bug/get_list?submit_begin_time=&submit_end_time=` 时间窗过滤正常；`bug/add` 空 payload → code 51「缺少项目ID」有参数校验。
- 数据 ✅ 明细 6 条 = 总览 BUG 总数 6 ✔。

### 8. QA 总结与关注事项（可编辑，写链路）✅满足

各角色问题总结 / BUG总结分析 / BUG改进建议 / 重点关注事项 / 测试流程改进，QA 可编辑，按统计周期独立。

验证证据：
- UI ✅ 点「编辑」进编辑态（加一行 / 保存 / 取消），真实输入内容→保存→Toast「保存成功」→回显。
- 边界 ✅ 空内容行可保存（title 空 + content 默认结构）。
- 接口 ✅ 写入走 `data_export/add_qa_stat_summary_item`，payload 含 `period_type/period_key/section=role_summary/sub_key/content`。
- 数据 ✅ **落库持久 + 周期独立**：保存后刷新页面仍在（真落库）；切「季度」该内容不显示、切回「月」重现（QA 总结按统计周期隔离）✔。
- 备注：验收产生的测试行已在验收结束时删除，库内无残留。

### 9. 导出长图 ✅满足

顶部「导出长图」按钮，前端 `modern-screenshot` 将总览区渲染为 PNG 下载。

验证证据：
- UI ✅ 点击后 `exportingReport` 置 true→执行→false，结束 Toast「长图已导出（收起的分区不会出现在图中）」。
- 边界 ✅ 导出前对区内 ECharts 关动画重绘到终态（防停在入场动画中间帧）；隐藏编辑按钮类控件。
- 接口 — 纯前端截图，无后端接口。
- 数据 ⚠️ headless 环境下未捕获到 download 事件（脚本 `a.click()` 触发下载在集成/无头浏览器沙箱可能不落盘），但**方法执行完整且成功 Toast 出现**，判为功能正常；真实浏览器可正常下载（建议人工在真实 Chrome 复核一次落盘文件）。

---

## 二、缺陷 BUG

> B1（BUG归属筛选清零递交维度）已于 2026-07-27 修复销账（复验通过，见「复验记录」），此处只列未修复项；B1 残留的 KPI 环比卡问题见 fix-handoff.md「B1-残留」。

| # | 缺陷 | 严重度 | 状态 |
| --- | --- | --- | --- |
| B2 | **「临时递交」跨模块数据不对齐**：看板临时=12，递交列表模块 `linshi_status=1` 的 7 月记录=17，同一概念两套数字，用户跨页核对必对不上。 | 中（数据正确性） | 未修复；复现/定位/通过标准见 fix-handoff.md B2（另：2026-07-27 观察到临时占比 18.46%→32.31% 变动，疑似开发正在调整此口径） |
| B3 | **补录递交（无计划时间）在「递交列表」模块不可见**：看板 65 条中 5 条 `plan_publish_time` 为空（4 超时+1 延期），递交列表按计划时间筛选任何日期范围都查不出，用户从列表侧核对看板必差 5 条。 | 轻微（看板统计本身正确） | 未修复；见 fix-handoff.md B3 |
| B4 | **看板侧时间字段显示错值 `2006-01-02 15:04:05`**：下钻明细「提交时间」与延期清单「实际递交」列均显示 Go 零值时间，而源头 `bug/get_list` 的同一条记录时间正确（如 #546=2026-07-03）——看板展示与源数据不对齐。 | 中（不影响统计数字，但明细列误导） | 未修复；见 fix-handoff.md B4 |

---

## 三、风险 Risk

> 注：原 R1（时间错值）/R7（临时口径）/R8（补录不可见）属实锤数据不对齐，已升级为缺陷 B4/B2/B3（§二）。

| # | 风险 | 影响×可能性 | 说明 / 建议 |
| --- | --- | --- | --- |
| R2 | **递交口径「延期 vs 超时」并存待确认** | 低×中 | KPI 中 延期递交=1、超时递交=4 为两个独立指标；递交总数 65 = 按时 63 + 延期 1 + (超时 4 是否与按时/延期交叉计数) 口径未在页面标注。建议补充口径说明或经产品确认三者边界。 |
| R3 | **「添加Bug」按钮首屏短暂 disabled** | 低×中 | `isCanAddBug` 依赖 `test_user_list`，首屏该列表未加载完时按钮 disabled，`test_user_list.map` 曾在早期渲染期抛 `Cannot read 'map' of undefined`（见 R4）。数据就绪后恢复可点。建议 `isCanAddBug` 对 `test_user_list` 做空数组兜底。 |
| R4 | **组件初次挂载期控制台报错（HMR/首帧竞态）** | 低×低 | 首次进页面偶发一批 Vue 报错：`ReferenceError: currentPeriodOf is not defined`（data()）、`activeMainTab is not defined`、`isCanAddBug` 读 undefined.map、`getBugList` 设 undefined.page。稳定后（正常菜单导航/刷新）**不复现**，判为开发态 HMR 残留 + 首帧数据未就绪竞态，生产构建需回归确认。建议核对 `currentPeriodOf`/`activeMainTab` 是否为遗漏定义的变量。 |
| R5 | **非 QA 用户后端写权限未实操验证** | 低×中 | 前端「添加Bug」用白名单门禁（展示层）；后端 `bug/add` 空 payload 仅返「缺少项目ID」（参数校验），**未实锤后端是否对非 QA 用户做权限校验**（为避免向真实项目写入 BUG 未走完整 add）。建议后端确认 bug/add 有身份/角色校验。 |
| R6 | **导出长图 headless 未落盘** | 低×低 | 见 §一.9 数据行，真实浏览器建议人工复核一次下载文件完整性（尤其中文字形，代码注释提到不内嵌 webfont、靠系统字体）。 |


---

## 附录 A：验收数据与回归

### A.1 回归明细（全绿）

| 选集 | 结果 |
| --- | --- |
| `--grep "@project_publish"`（递交簇，看板递交维度共享） | 12 passed (1.2m) |
| `--grep "@data_export"`（差集纠偏：看板走 data_export 命名空间） | 5 passed (50s) |
| api 冒烟 `tests/api-*.spec.js` | 37 passed (18.5s) |

回归结论：本项功能共享 `data_export`（QA 统计接口全在此命名空间）与 `project_publish`（递交数据）两簇，两簇老功能回归全绿，无被改坏迹象。

### A.2 核心接口清单

| 用途 | 接口 |
| --- | --- |
| 关键指标环比 | `GET data_export/get_qa_stat_kpi?period_type&period_key&dept_id&assigned_to&tester&pm_id` |
| BUG 维度 | `GET data_export/get_qa_stat_bug?…`（8 维度键） |
| 递交维度 | `GET data_export/get_qa_stat_publish?…`（overview 与 KPI 同源） |
| QA 总结（读） | `GET data_export/get_qa_stat_summary?period_type&period_key` |
| QA 总结（写） | `POST data_export/add_qa_stat_summary_item`（section/sub_key/content） |
| 图表下钻明细 | `GET data_export/get_qa_stat_detail_list?…&chart_key&option_id&page&limit` |
| BUG 源头明细 | `GET bug/get_list?submit_begin_time&submit_end_time&…` |
| BUG 常量 | `GET bug/get_const` |
| BUG 新增 | `POST bug/add`（缺 project_id → code 51） |

边界实测：非法 `period_type=abc` → code 50「不支持的统计周期类型」；`period_key=9999-99` → code 50「月周期格式错误」；空 period_key → code 51「请选择统计周期」；非法 `chart_key` → code 50「无效的 chart_key」；不存在 assigned_to → code 0 返全 0（不报 500）。

### A.3 验收产生的测试数据

- QA 总结「各角色问题总结」造 1 行测试内容（`add_qa_stat_summary_item`，2026-07 月周期）→ **验收结束已删除，库内无残留**。
- 无其他写入（越权 bug/add 未走完整提交）。

### A.4 四周期数据核验明细（接口直调逐周期算数核对）

| 周期 | 递交总数 | 正常/提前/临时/延期/超时 | 率自洽 | 按时=正常+提前 | BUG维度(total=KPI=状态段和) | 环比基准 |
| --- | --- | --- | --- | --- | --- | --- |
| 月 2026-07（进行中） | 65 | 50/13/12/1/4 | ✔ | 63=63 ✔ | 6=6=6 · 浓度 6/5=1.2 | prev 74.32 = 上月实测 ✔ |
| 月 2026-06（已结束） | 74 | 55/10/14/8/1 | ✔ | 65=65 ✔ | 6=6=6 · 浓度 6/4=1.5 | ✔ |
| 季度 2026Q3（进行中） | 65 | 50/13/12/1/4 | ✔ | 63=63 ✔ | 6=6=6 | prev 76.67 = 上季实测 ✔ |
| 季度 2026Q2（已结束） | 240 | 184/30/31/17/13 | ✔ | 214=214 ✔ | 19=19=19 · 浓度 19/13=1.46 | ✔ |
| 半年 2026H1 | 430 | 331/51/60/33/13 | ✔ | 382=382 ✔ | 43=43=43 · 浓度 43/28=1.54 | ✔ |
| 半年 2026H2 | 65 | 50/13/12/1/4 | ✔ | 63=63 ✔ | 6=6=6 | prev 76.98 = 上半年实测 ✔ |
| 年 2026 | 495 | 381/64/72/34/17 | ✔ | 445=445 ✔ | 49=49=49 · 浓度 49/32=1.53 | prev 71.84 = 上年实测 ✔ |
| 年 2025 | 1552 | 1115/101/635/78/0 | ✔ | 1216=1216 ✔ | 295=295=295 · 浓度 295/113=2.61 | ✔ |

结论：**月 / 季度 / 半年 / 年 四种粒度全部筛选正确**。每周期均满足四道校验（KPI↔PUBLISH 两接口同源、率手算=展示、按时=正常+提前、BUG维度三方一致），且四种粒度的环比基准链均正确指向上一同粒度周期（无错位）。合理巧合：2026-07 = 2026Q3 = 2026H2 数值相同（今日 7/24，Q3/H2 目前仅累计到 7 月）。

### A.5 明细图表数据核验（各分布数组加总 + 逐行率，2026-07）

BUG 维度全部分布数组，各项加总 = BUG总数 6，且每项堆积段（致命+严重+一般+建议）= 该项 total：

| 分布 | 核验 |
| --- | --- |
| 类型分布（bug_type_distribution） | 3 项和 = 6 ✔ |
| 自检分布（self_check_distribution） | 3 项和 = 6 ✔ |
| 部门 TOP5（dept_top5） | 3+1+1+1 = 6 ✔，堆积段=total ✔ |
| 指派人 TOP10（assignee_top10） | 2+1+1+1+1 = 6 ✔ |
| PM 堆积（pm_stacks） | 2+1+1+1+1 = 6 ✔ |
| 项目 TOP10（project_top10） | 2+1+1+1+1 = 6 ✔ |
| 行业线平均（h_line_avg_stacks） | 未设置行业线 avg 1.2 = bug 6 / 项目 5 ✔ |

递交维度明细：

| 明细 | 核验 |
| --- | --- |
| PM 递交表逐行率（6 位 PM） | 提前率/超时率/临时率 逐行手算全部 = 展示值 ✔（如晏凯 6/22=27.27%、张小杰超时 3/14=21.43%、郝宦程临时 2/3=66.67%）；PM 递交次数总和 22+14+13+7+6+3 = 65 = 递交总数 ✔ |
| 临时递交分布 | 7 类（临时反馈修改4/未填写3/研发修复BUG1/临时新增1/临时需求1/上周提醒后忘记1/临时变更1）和 = 12 = overview.temporary ✔ |
| 延期责任分布 | 中东团队 1 = delay_total 1 ✔ |
| 延期清单 | delay_total 1 = overview.delay 1，清单 1 条 ✔ |

---

## 附录 B：跨模块独立对账（2026-07-27，非看板自家接口，去源头模块取数）

> 方法：不信看板自家接口互证，去「递交列表」模块（`project_publish/get_list`）与「BUG 系统」（`bug/get_list`）独立拉全量记录、自行分组统计，与看板逐项对账。

### B.1 BUG 侧（BUG 系统独立分组 vs 看板）——**全对 ✔**

| 维度 | BUG系统独立统计 | 看板 | 结论 |
| --- | --- | --- | --- |
| 总数 | 6 | 6 | ✔ |
| 按指派人 | 龙顺波2 张炼1 鄢羽1 傅闽翌1 周洪磊1 | assignee_top10 同 | ✔ |
| 按项目 | 千岛湖2 托管农场1 天溯1 虹桥1 阿布扎比1 | project_top10 同 | ✔ |
| 按类型 | 程序功能3 场景美术2 项目技术1 | 类型饼图 同 | ✔ |
| 按状态 | 已解决6 | 状态段 同 | ✔ |
| 修复工时合计 | 3.5h | 3.5h（平均 0.58） | ✔ |
| 涉及项目数 | 5 | 缺陷浓度分母 5 | ✔ |

### B.2 递交侧（递交列表模块独立统计 vs 看板）——**总数可解释一致，临时口径分歧**

- 看板全部递交下钻 65 条（`chart_key=publish_status`）；递交列表模块按实际递交时间在 7 月的记录 = 60 条。
- **5 条差异精确定位**：#14260/14269/14290/14308（4条超时）+ #14286（1条延期）均为 `plan_publish_time` 为空的补录递交——列表页按计划时间筛选故拉不到（见 §二 B3）。**看板=列表超集，无遗漏无多算** ✔。
- PM 分组差额精确吻合：独立统计 晏凯22 曾豪12 张小杰11 李赵波6 陈蓓6 郝宦程3，加回 5 条补录（张小杰+3 曾豪+1 李赵波+1）= 看板 PM 表 22/14/13/7/6/3 ✔。
- 涉及项目：65 条按项目去重 = 40 = 看板卡 ✔；status 分布 正常50+提前13+PM递交1+延期1 = 65 ✔。
- 超时：看板 65 条明细内 `is_over_tb_time=1` 自算 = 4 = 超时卡 ✔（与 V2.2.8 的 12 条全年口径不冲突：那是全年，本月 4 条）。
- **临时：不一致 ✗** → 看板 12 vs 列表 `linshi_status=1` 17 条，判据两边不同，详见 §二 B2。

### B.3 本页筛选联动与区块间一致性（复验轮已覆盖）

- 筛选联动详见 §一.2 与「复验记录」：递交维度分析已不响应 BUG 归属筛选（恒 65）✔；KPI 递交环比卡仍响应（残留），同屏不一致 ✗（fix-handoff 第 2 轮）。
- 区块间：BUG总数=状态段和=类型饼图和=各TOP图加总（附录 A.5）✔；递交概览=延期清单=临时分布=PM表加总 ✔。

---

## 复验记录

| 日期 | B# | 结果 |
| --- | --- | --- |
| 2026-07-24 | B1（BUG归属筛选清零递交维度） | **部分修复**：「递交维度分析」区块已不受 BUG归属部门/归属人/PM 筛选影响（恒 65）✔；残留①顶部「关键指标环比概览-递交数据环比」仍被 BUG 归属筛选清零（0/0）、PM 筛选仍生效（22/22），与递交维度分析区同屏不一致；残留②无筛选时准时递交率口径变为 93.85%（61/65，疑似=总数−超时），与下方「按时递交 63」并存两种口径。详见 fix-handoff.md 复验记录与第 2 轮通过标准。 |
| 2026-07-27 | B1 主体销账 | 用户确认设计口径=「BUG 筛选只影响 BUG，递交只受统计周期影响」；实测选「项目开发」部门 → BUG维度=3 正确过滤、递交维度分析恒 65 ✔ → **B1 主体关闭**。小残留仍存：KPI 环比卡内准时递交率/正常率/延期数仍显示 0/0（超时卡已正常显示 4 项），待第 2 轮修复后逐卡核销。另：临时递交占比口径观察到变动（18.46%→32.31%），疑似开发正在处理 B2 临时口径，下轮一并确认。 |
