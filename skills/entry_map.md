# 51PM — 入口地图（实测确认的功能入口，全 51pm skill 共享）

> **本文件是唯一权威的入口登记处**，由验收/操作类 skill 在每轮实测后回填维护。任何 51pm skill 找功能入口时**先查这里**，查不到再现场找，找到后**必须回填**。

## 回填规则（对所有 51pm skill 生效）

1. **新确认的入口**（表里没有的）：验收/操作结束交付前，立即追加一行
2. **纠正的入口**（实测与表不符）：更新对应行，旧入口若易混淆保留 ⚠️ 备注
3. **同入口踩到的坑**（静默失败、旧按钮混淆、权限差异等）：以 ⚠️ 写进备注列
4. 入口写法统一：「一级菜单 → 二级页面 → 具体控件」，悬停触发标注 `(悬停)`

## 已确认入口（V2.2.3 / V2.2.4 两轮验收实测）

| 功能 | 实测入口 | 备注 |
|---|---|---|
| 日报导出 | 「任务」→ 任务行「查看日报」→ 日报弹窗右上角「导出」（蓝色下载图标） | ⚠️「统计 → 项目成本」页也有个旧导出按钮，不是同一功能，且点击无反馈 |
| 递交列表 → 项目跳转 | 「递交」→ 递交列表 → 点项目名 → 项目概况 | |
| 批量创建任务（独立任务） | 「非项目」→ 需求 → 创建任务 → 独立任务 | ⚠️ 无需求上下文直接提交会静默失败（code 51 不透出），URL 需带 demandId |
| 批量添加反馈 | 「我的地盘」→ 我的反馈 → 添加反馈 → 批量添加反馈 | |
| 主题面板 | 顶部导航 🍦 图标 → 主题面板 | 18 个主题：10 常规 + 8 节日 + 2 特效开关 |
| 组群配置 | 「我的工作台 → 组群配置」 | |
| 从组群导入成员 | 「非项目 → 会议工时 → 需求 → 创建任务(悬停) → 多人通用任务 → 从组群导入」 | |
| 项目测试文档 | 「项目 → 项目列表 → 点项目名进项目概况」右侧信息栏 | 位于『定制接口文档』与『行业接口文档』之间 |
| 排期表搜索项目 | 「排期 → 排期表」顶部「搜索项目」筛选 | |
| 自由需求 | 「非项目 → 日常管理 → 创建需求(悬停) → F自由需求」 | |
| 项目递交（项目内） | 「项目 → 项目详情 → 二级菜单『项目递交』」，路由 `/project/project_publish?projectId=N` | V2.2.5 新增；⚠️顶部按时/超时/延期/待递交数字块是纯统计不可点击筛选；「最新/最早」排序可用；⚠️接口 `project_publish/get_list` 的 `project_id` **GET 直调时不参与过滤**（任意 id 返全量 12810 条，V2.2.6 轮接口测试发现；页面内实际取数方式另有通道，接口回归断言不要依赖该参数）；keywords 参数按项目名精确匹配 |
| 项目需求页 | 「项目 → 项目详情 → 二级菜单『项目需求』」，路由 `/project/demand?projectId=N` | ⚠️二级菜单点击不路由的坑 V2.2.5-copilot 轮**未复现**（真实点击直接路由成功），`location.href` 导航仍可作兑底；需求描述列「查看详情」按钮首次点击常无响应需点第二次；创建需求性质选「其他(会议、产研)」后名称变模板下拉(含「项目会议」) |
| 需求拆分（拆解为任务） | 项目需求页 → 需求行操作列 el-icon-menu 按钮 → 需求拆解弹窗（BatchCreateDialog 两步向导） | 任务选项面板=task-options-panel-popover；⚠️拆解任务工时总和>父需求标准工时会被 preSubmit 拦（toast 偶发不弹，表现为点了没反应）；默认带任务2/3/4空占位卡，用 .bcd-tab__del 删除；⚠️el-icon-menu 固定列双份渲染，第一个不可见，要过滤 offsetWidth>0；任务选项面板「搜索任务选项」只搜当前分组（岗位专业/管理会议两分组要先切）；⚠️Escape 会关掉整个向导弹窗且不保留已填内容 |
| 创建任务-快速建组群 | 多人通用任务弹窗 → 「从组群导入」popover → 『立即管理』链接 → 就地弹「组群配置」弹窗 | V2.2.5；⚠️V2.2.5-copilot轮嵌套弹窗成员 el-select 真实点击一次就展开（旧 vm 兑底方案未用上）；坑：el-select 多选框外层 input 会被 tags 里 mini input 拦截，点 `.el-select__input.is-mini/.is-small` 才有效；新建组群后自动选中，加人→「加入」→「保存成员」两步都要点 |
| 我的任务（日历） | 「我的地盘 → 左侧栏『我的任务』」，路由 `/my_board/main/task` | ⚠️直接 location.href 会被重定向回 main，必须点左侧菜单进入；日历格 class `tc-cell`（今天 is-today）；V2.2.5 起点任务项直接弹花费填写弹窗，右侧 tc-panel__body 侧栏保留 |
| 填写工时（含Ctrl+V贴图） | 任务列表行 button.workHour（CDP click 可能无效，用原生 btn.click()）→ 工时记录弹窗 → 「填写工时」 | 上传区组件 UploadImages.vue，V2.2.5 支持粘贴图片（合成 ClipboardEvent+DataTransfer 可自动化验证）；多任务选项任务每子项有独立 .option-hour-input 工时框+描述 textarea，仅工时>0 的子项必填描述 |
| 申请发包（模型外包） | 「项目 → 项目详情 → 『模型外包』→ 申请发包」按钮，路由 `/project/outsource_project?projectId=N` | 发包类型是**多选** el-select；列表有「全部/内部自制」tab；接口 `outsource/create_package` / `get_package_list?sj_num=`；⚠️V2.2.5-copilot轮真实点击均有效（旧记录“CDP click 无效”未复现）；弹窗双份渲染会互遮，用快照 ref 或取最后一份可见 dialog |
| 发包审核/立项（自制流程） | 模型外包列表每个发包的独立操作行「审核/立项/编辑」（每发包占数据行+标题行+操作行三行，索引会错位，直接找按钮文本） | V2.2.5；立项弹窗双模式：报价单拆解(仅外包) / 手动创建(可指派自制)；自制选「选择制作组(自制)」radio → 级联选部门/人(如 DTA资产/李润芝)；立项后状态变「制作中」+「自制」标签+「前往管理」+「打分」按钮；打分=「结项评价」弹窗(无完工任务时空态)；🐛发包详情页(`outsource_detail?outsourcePackageId=N`)「任务管理」按钮点击/悬停均无响应(V2.2.5-copilot轮发现) |
| 我的任务日历-任务卡备注 | 「我的地盘 → 我的任务(日历)」点日历格 → 右侧任务卡片「备注」字段 | V2.2.6 新增：备注=任务描述原文（DTA 任务全名）；⚠️「添加工时」弹窗内部不显示任务描述 |
| TB 递交申请 | 「我的地盘 → TB/BD仪表盘 → 立即申请递交」或「我的递交 → 申请递交」，路由 `/my_board/main/tb_publish` | V2.2.6 轮确认；⚠️直接 URL 会被重定向回 main，必须点左侧菜单（同「我的任务」坑）；声明 checkbox 要真实鼠标点击，JS click 无效 |
| PM 审批递交申请 | 「我的地盘 → 我的信箱 → 审批递交申请(PM) tab」→ 行尾「通过审批」→「转化为递交」表单 | V2.2.6 轮确认；⚠️「通过审批/驳回」按钮 disabled 条件：`systemRole!=='PM'`（另有硬编码白名单彰中豪）；⚠️小视口(<1920)下左侧菜单不渲染 |
| QA 递交（状态自动判定） | 「递交」→ 列表行「QA递交」→ 项目递交弹窗「项目递交(QA)」tab | V2.2.6：打开时递交状态自动预选（早≥1天→提前递交，否则正常递交；晚于不自动选延期）；⚠️QA递交/编辑按钮由用户名白名单 `testListRooters` 控制非项目 QA 角色；⚠️递交列表默认只筛今天，日期筛选要走 vm form+searchData()（fill 不触发） |
| 会议动态待办负责人 | 「项目 → 项目动态 → 添加动态（会议动态）→ 待办事项」每条带 状态+相关人员 | V2.2.6 新增；状态：待处理/进行中/已完成；⚠️相关人员 el-select 要点 `.el-select__input.is-mini`（tags 拦截坑同组群弹窗）；接口 `project_moment/get_list?project_id=N&type=1`（type=1 会议动态，返回待办含负责人+状态字段，可作落库断言） |
| 行业接口文档 | 「项目 → 项目概况」右侧项目信息栏「上传行业接口文档」 | V2.2.6 新增；与定制接口文档独立、互不覆盖；上传弹窗选文件后还要点「确认上传」 |
| 产能数据看板 | 「统计 → 产能数据看板」，路由 `/statistic/capacity_analysis`；页头「工程产能分析 / 小组产能看板」点击切换 | V2.2.6 新增；色块 selector `.bar-seg--clickable`（--doing/--wait/--pause），点击下钻需求列表；接口 `data_export/get_employee_project_list?dept_id[]=&start_date=&end_date=&filter_type=all`（⚠️非法日期静默兜底不报错、不存在部门返空数组——边界断言只验不 5xx）；非生产成员过滤名单刷新恢复默认 |

## 页面等待锚点（待回填）

Element UI 表格/图表加载慢的页面，记录可靠的 `wait_for_selector` 锚点：

| 页面 | 锚点 selector | 建议 timeout |
|---|---|---|
| （待首次回填） | | |
