# 模型数据看板【只读】

> 一句话：看模型发包/资产的总览指标与明细，区分供应商与自制口径。

- **入口**：统计 → 「资产」分组 → 模型数据看板；页头「模型数据总览 / 模型明细」切换
- **路由**：`/statistic/outsource_panel`
- **适用/触发**：量化模型制作成本、供应商交付质量

## 操作步骤（真实 UI）

1. 进总览页：发包数量/发包金额/资产数量/资产评分/不合格占比/平均反馈数六卡 + 金额、打分、类型状态、属性质量、省份热力、评分档位等图表
2. 交叉验算（如发包 272 = 供应商 150 + 自制 122；金额 376.8 万 = 261.5 + 115.3）
3. 切「模型明细」：发包维度(272) / 资产维度(316) 切换，支持自制/外包筛选

## 可直调接口

- 总览 `outsource/get_data_overview?period=&producer_scope=&score_scope=`（非法 period/空参静默兜底返全量）
- 明细 `get_package_dimension_list`（非法 status 返 0 条）/ `get_asset_dimension_list`

## 已知坑

- 🐛 `get_asset_dimension_list?limit=-5` → HTTP 500（slice bounds out of range，负数 limit panic，待修复）
- ⚠️ 资产状态四档之和 278 ≠ 资产总数 316（差 38，疑漏未开工档，待产品确认）

---
_来源：V2.2.8 验收轮 ｜ 最后验证：2026-07-18_
