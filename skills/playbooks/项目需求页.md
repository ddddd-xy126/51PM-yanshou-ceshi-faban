# 项目需求页 — 查看/需求任务列表/自动暂停【只读】

> 一句话：查项目需求列表及状态，点需求名看其任务列表（含离职人员历史信息），了解「超2周自动暂停」口径。

- **入口**：项目 → 项目详情 → 二级菜单「项目需求」
- **路由**：`/project/demand?projectId=N`
- **适用/触发**：查需求状态分布、看某需求下任务、核对暂停中需求

## 操作步骤（真实 UI）

1. 进项目需求页（⚠️ 二级菜单点击不路由的坑 V2.2.5 起未复现，`location.href` 可兜底）
2. 需求列表「完工状态」列：进行中/未开工/已完成/**暂停中**
   - 「需求描述」列首次点「查看详情」常无响应，需点第二次
3. 点需求名（`span.longText.title.link`）→「需求:xxx」任务列表弹窗（指派给/审核人/工时列）
   - ⚠️ 固定列双份渲染，第一个 span 不可见需过滤 `offsetWidth>0`
   - 离职人员历史任务的「指派给」列**仍正常显示姓名**（V2.2.7 修复点，不再空白）

## 「超2周自动暂停」口径（V2.2.8）

- 项目下**进行中的新增/变更/初始需求**，若近 2 周无新增任务且无填工时 → 自动转 `pause`（UI 显「暂停中」）
- 样本：贡井区国资管理平台(#6690) 需求 #47299/#47289
- ⚠️ 定时任务触发周期与「两周内是否填过工时」的排除逻辑无法从前端验证

## 可直调接口

- `demand/get_project_demand_list?project_id=N&limit=&page=`：status 枚举 `doing/done/wait/pause`
- `project_task/get_task_list_by_demand_id?demand_id=N`：返回 `assigned_to` 人员 id（离职人员 id 完整保留）；空参 code 51
- 离职人员识别 = `user/get_user_select_list`（在职）与 `get_all=true`（全量）的差集

## 已知坑

- ⚠️ 需求描述「查看详情」首次点常无响应，点第二次
- ⚠️ 任务列表弹窗固定列双份渲染，过滤 `offsetWidth>0`
- ⚠️ 列宽拖动排版错位 BUG 已于 V2.2.7 修复（连拖/来回缩放/横滚/刷新均正常）

---
_来源：V2.2.5 / V2.2.7 / V2.2.8 验收轮 ｜ 最后验证：2026-07-18_
